
_format_version: "3.0"

# Admin protection
consumers:
  - username: admin
    keyauth_credentials:
      - key: 17c22bc007cc2a3d67ebeae3bc9846e57b7e101cb91bb36101e685e5d671bc5c

# Protect Kong's own Admin API via proxy
services:
  - name: admin-api
    url: http://kong-cp:8001
    routes:
      - name: admin-api-route
        paths:
          - /admin
    plugins:
      - name: key-auth

